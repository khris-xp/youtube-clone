import React from 'react';
import { Fragment } from 'react';
import { Id } from '../interfaces/video';

const VideoCard: React.FC<Id> = ({ videoId }) => {
  return <div>VideoCard</div>;
};

export default VideoCard;
